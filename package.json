{
  "name": "sacred-wisdom-cross",
  "version": "1.0.0",
  "description": "Multi-faith wisdom application with Cloudflare Worker backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "scrape": "python scraper.py",
    "format": "prettier --write '**/*.{js,json,html,css}'"
  },
  "dependencies": {
    "gsap": "^3.12.2"
  },
  "devDependencies": {
    "wrangler": "^3.15.0",
    "prettier": "^3.1.0",
    "eslint": "^8.56.0"
  },
  "browserslist": [
    "last 2 Chrome versions",
    "last 2 Firefox versions"
  ],
  "private": true,
  "workspaces": [
    "frontend",
    "worker"
  ]
}
